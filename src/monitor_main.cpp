#include <time.h>
#include <thread>
#include <chrono>
#include <queue>
#include <condition_variable>
#include <functional>
#include "robotinfo.h"

#define 	NODENAME			"Monitor"


int main(int argc, char** argv)
{
	bool shutdown = false;
	RobotInfo* pri = RobotInfo::GetInstance();
	return 0;
}
